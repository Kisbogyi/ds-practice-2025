syntax = "proto3";

package fraud_detection;

service FraudDetectionService{
    rpc CheckFraud (FraudRequest) returns (FraudResponse);
}

message FraudRequest {
    string username = 1;
    int32 order_amount = 2;
    string billing_address = 3;
}

message FraudResponse {
    bool is_fraud = 1;
}
