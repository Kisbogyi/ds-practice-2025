syntax = "proto3";

package broadcast;

service BroadcastService {
    rpc Broadcast (Message) returns (Empty);
}

message Message{
  string order_id = 1;
  repeated int32 vector_clock = 2;
}

message Empty{
}
